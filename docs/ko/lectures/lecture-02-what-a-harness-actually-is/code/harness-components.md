# ハーネス構成要素の例

ローカルの `repository` で作業する `coding agent` の場合:

- モデル:
  LLM そのもの

- ハーネス(`harness`):
  - システムプロンプト
  - `AGENTS.md`
  - `bash` ツール
  - ファイル読み書きツール
  - `git` アクセス
  - ローカルファイルシステム
  - 起動スクリプト
  - テストコマンド
  - 停止フック(`hook`)
  - リントチェック
  - 評価者ループ

上のハーネス要素のどれか1つでも変更すると、実際に動くエージェントは変わります。
