# パスゲートポリシー(Pass Gate Policy)

機能を `passes: false` から `passes: true` に移すには、次の条件をすべて満たしている必要があります。

- 想定されたワークフローが実行(exercise)されていること
- 成功の証拠が記録されていること
- テスト済みの経路に、実行を妨げるエラーがないこと
- 実装によってアプリが壊れた状態や曖昧な状態のままにならないこと
