# 例: レビューのフィードバックをルール化する

レビューで繰り返し指摘されたコメント:

> レンダラーからファイルシステムユーティリティを呼び出さないでください。preload ブリッジを使用してください。

harness のルールに昇格させる:

- linter または import のルールを追加し、レンダラーコードでの `fs` の使用を禁止する
- preload の境界を説明する案内文を追加する
